# ============================================================================
# Database Configuration
# ============================================================================
# 
# Choose ONE of the following options:
#
# Option 1: Production/Render Database (Recommended for production)
# ----------------------------------------------------------------------------
# Set DATABASE_URL to use your production database (e.g., Render PostgreSQL)
# This takes priority over individual DB_* variables below
#
# DATABASE_URL=postgresql://user:password@host:port/database
#
# Example Render database URL format:
# DATABASE_URL=postgresql://news_aggregator_user:password@dpg-xxxxx-a.oregon-postgres.render.com/news_aggregator_db
#
# Option 2: Local Development Database
# ----------------------------------------------------------------------------
# Use individual variables for local Docker PostgreSQL
# Only used if DATABASE_URL is not set
#
DB_HOST=localhost
DB_PORT=5432
DB_NAME=news_aggregator
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================================================
# OpenAI Configuration
# ============================================================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# Gmail SMTP Configuration (for email sending)
# ============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_16_char_app_password
FROM_EMAIL=your_email@gmail.com

# ============================================================================
# Optional: YouTube Proxy Configuration
# ============================================================================
# YOUTUBE_PROXY_USERNAME=your_proxy_username
# YOUTUBE_PROXY_PASSWORD=your_proxy_password
